{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "popout",
  "description": "Capability for pop-out bible pane windows",
  "windows": ["popout-*"],
  "permissions": [
    "core:default",
    "store:default",
    "fs:default",
    "fs:allow-appdata-read-recursive",
    "fs:allow-appdata-write-recursive"
  ]
}
